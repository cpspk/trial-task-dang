import { JsonValue } from '@prisma/client/runtime/library'
import Iframe from 'react-iframe'

export const Embed = (widgetConfig: JsonValue) => {
  return (
    // <Iframe
    //   url={widgetConfig?.url}
    //   className='w-full h-full'
    //   display='block'
    //   position='relative'
    // />
    <div>

    </div>
  )
}